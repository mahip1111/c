// (int n, i=1)  i ki value 1 and n ki value by default garbage value ya apko leni hogi user se.



#include <stdio.h>

int main(){
    int i,a;
    i=a=10;
    printf("The value of i is %d and the value of a is %d", i,a);
    return 0;
}
// This is the short way to give the two variables same value.
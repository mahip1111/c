/*C language me “conditional operator” technically sirf ek hi hota hai — ternary operator (?:).
Isko conditional operator isliye kehte hain kyunki ye condition ke basis par decide karta hai ki kaunsi value return hogi.*/

// Keywords are predefined reserved words in C used to write programs, and they cannot be used as identifiers. There are total of 32 keywords in c language some of them are:- if, else, int and so. You cannot use keywords as variable names, function names, or identifiers. They are predefined and part of the C language syntax — used to perform specific tasks.

// Characteristics of Keywords
// All keywords are written in lowercase letters.
// They cannot be redefined or used as variable names.
// Each keyword has a specific purpose in the program.

// Rules of identifiers:-
// first character must be alphabet or underscore
// must consist only of letters digits and undescore
// only first 31 characters are significant
// cannot be a keyword.
// must not contain white space

// 💡 Summary:
// Keyword → fixed reserved words
// Identifier → programmer-defined names

// typdef int marks => now you can use marks as an int variable.

// ++x= pre increment and x++ is post increment

// A unary expression is an expression that operates on only one operand using a unary operator.In other words, it applies an operation to a single variable or value. Some common such operators are +, ++, +=, *=, and so.

// sara operators ka name with example ek jagha tariqa se likho.

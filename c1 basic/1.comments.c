#include <stdio.h>

int main(){
    printf("GOOD MORNIG EVERYONE.");
    /*88 IIF /*BHF*/
    return 0;
}

// NOTE: Once the compiler finds an opening token(/*), it ignores everything until it find a closing token(*/).

// matlab ki jab tak aap end ma */ ya nahi daloga tab tak compiler starting token ka badh wali chizon ko ignore karega
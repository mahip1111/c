// shotere method to solve the problem 7.

#include <stdio.h>

int main() {

    int n;
    printf("Enter the number for which you want to calculate the sum of the numbers occurring in its multiplication table: ");
    scanf("%d", &n);
    printf("The required sum is:- %d", 55*n);
    return 0;
}
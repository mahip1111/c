// how to decide the output while using the modulus operator ?

// The sign of the result in a modulus operation (%) depends on the sign of the dividend (the numerator) — not the divisor (the denominator).
// 💡 Rule:
// For an expreression:
// a%b
// The sign of the result is the same as the sign of a (the dividend).

// EXAMPLES: 7%3=1, -7%3=-1, 7%-3=1, -7%-3=-1

// NOTE: a%b=a if a<b   and if a>b then you know.

// NOTE: a%(-b)= a%b

// NOTE: (-a)%b = (-a)%(-b) = -(a%b)